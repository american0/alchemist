<%= simple_form_for(@booking) do |f| %>
  <%= f.error_notification %>


  <div class="form-inputs">
    <%= f.input :date %>
    <%= f.input :address %>
    <!-- f.hidden_field :offer_id, value: current_user.id  -->
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
